<footer class="site-footer">
  <p class="text">
    {% assign CurrentYear = site.time | date: '%Y' | append: " " %}
    {% assign SiteName = site.title % | append: " " %}
    {{ "Copyright © " | append: CurrentYear | apppend: SiteName | append: ". All rights reserved." }}</p>
  <div class="footer-icons">
    <ul>
      <!-- Social icons from Font Awesome, if enabled -->
      {% include social/icons.liquid %}
    </ul>
  </div>
</footer>